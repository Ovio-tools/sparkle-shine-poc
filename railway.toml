# Default Railway config shared by the scheduled job services.
# The always-on simulation worker should use /railway.simulation.toml
# via the service's Custom Config File setting in Railway.
[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
