# Backward-compatible alias for the simulation worker.
#
# New deployments should prefer /railway.worker.toml so both always-on workers
# (simulation-engine and token-keeper) share the same restart policy. Keeping
# this file avoids breaking existing Railway Custom Config File references.

[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ALWAYS"
